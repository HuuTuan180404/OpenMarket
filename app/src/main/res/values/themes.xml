<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OuMarket" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.OuMarket" parent="Base.Theme.OuMarket" />

    <style name="Theme.OuMarket.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Customer.Logo" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/logo</item>
    </style>


    <style name="Theme.OuMarket.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.OuMarket.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ToolbarTitleTextAppearance" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:fontFamily">@font/satoshi_variable</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/den</item>
    </style>

    <style name="Customer.Theme.SearchView" parent="">
        <item name="titleCentered">true</item>
        <item name="fontFamily">@font/satoshi_variable</item>
        <item name="android:editTextColor">@color/trang</item>
        <item name="android:textColorHint">@color/trang</item>
    </style>

    <style name="Customer.ToolBar" parent="">
        <item name="titleCentered">true</item>
        <item name="fontFamily">@font/satoshi_variable</item>
        <item name="titleTextColor">@color/den</item>
        <item name="android:textColorHint">@color/den</item>
    </style>

    <style name="Customer.TextView" parent="">
        <item name="fontFamily">@font/satoshi_variable</item>
        <item name="android:textSize">20dp</item>
    </style>

    <style name="toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="collapseIcon">@drawable/im_menu</item>
        <item name="icon">@drawable/im_menu</item>
        <item name="titleTextColor">@color/den</item>
        <item name="fontFamily">@font/satoshi_variable</item>
    </style>

    <style name="font_tab_myOrder" parent="TextAppearance.Design.Tab">
        <item name="android:fontFamily">@font/satoshi_variable</item>
        <item name="android:textSize">20sp</item>
    </style>

</resources>